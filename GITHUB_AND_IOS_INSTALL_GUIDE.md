# دليل رفع المشروع إلى GitHub وبناء وتثبيت حزمة iOS (Runner) على الآيفون

تم إعداد وتجهيز مشروع **Admin (لوحة تحكم الإدارة)** بالكامل ليكون آمناً ومحمياً وجاهزاً للرفع على منصة **GitHub**، مع مسار عمل مؤتمت بالكامل (**GitHub Actions CI/CD**) لبناء نسخة نظام iOS واستخراج ملفات **`Runner.ipa`** و **`Runner.app`** لتثبيتها على جهاز الآيفون الشخصي بسهولة وبدون الحاجة إلى حساب مطور مدفوع من Apple.

---

## 📑 فهرس الدليل
1. [ما تم تنفيذه وتأمينه في المشروع](#1-ما-تم-تنفيذه-وتأمينه-في-المشروع)
2. [الخطوة الأولى: رفع المشروع إلى مستودع GitHub](#2-الخطوة-الأولى-رفع-المشروع-إلى-مستودع-github)
3. [الخطوة الثانية: ضبط أسرار المستودع (GitHub Secrets)](#3-الخطوة-الثانية-ضبط-أسرار-المستودع-github-secrets)
4. [الخطوة الثالثة: تشغيل مسار البناء (GitHub Actions)](#4-الخطوة-الثالثة-تشغيل-مسار-البناء-github-actions)
5. [الخطوة الرابعة: تنزيل ملفات الـ Artifacts (Runner.ipa / Runner.app)](#5-الخطوة-الرابعة-تنزيل-ملفات-الـ-artifacts)
6. [الخطوة الخامسة: تثبيت التطبيق على جهاز الآيفون مجاناً](#6-الخطوة-الخامسة-تثبيت-التطبيق-على-جهاز-الآيفون-مجاناً)
7. [الخطوة السادسة: تفعيل صلاحية المطور على الآيفون (مرة واحدة)](#7-الخطوة-السادسة-تفعيل-صلاحية-المطور-على-الآيفون)
8. [التشغيل والتطوير المحلي على جهازك](#8-التشغيل-والتطوير-المحلي-على-جهازك)

---

## 1. ما تم تنفيذه وتأمينه في المشروع

| البند | التعديل الذي تم | الفائدة الأمنية والتقنية |
| :--- | :--- | :--- |
| **عزل المفاتيح الحساسة** | نقل توكن Mapbox ومفاتيح السيرفر إلى `AppEnv` (`String.fromEnvironment`) | منع تسريب التوكنات على المستودعات العامة والخاصة |
| **قالب البيئة `.env.example`** | تحديث القالب ليوضح كافة المفاتيح المطلوبة بدون قيم حساسة | جاهز للرفع على GitHub ليعرف المطورون المتغيرات المطلوبة |
| **ملف الحماية `.gitignore`** | استبعاد `.env` وشهادات التوقيع وملفات Firebase الحساسة ومجلدات الـ IPA | حماية تامة تمنع رفع الأسرار أو مخلفات البناء |
| **إعدادات iOS Podfile** | إنشاء `ios/Podfile` مع استهداف `iOS 16.0` و `use_frameworks!` | توافق كامل مع خوادم Apple Silicon وبناء خالٍ من الأخطاء |
| **أذونات Info.plist** | إضافة أذونات الكاميرا، المعرض، الموقع، والشبكة المفتوحة | منع انهيار التطبيق المفاجئ على نظام iOS عند استخدام الخرائط والصور |
| **تهيئة Firebase الآمنة** | إنشاء `firebase_options.dart` وتحديث `initialServices` | تشغيل التطبيق على iOS دون الحاجة لوجود ملف plist محلي |
| **مسار GitHub Actions** | إنشاء `.github/workflows/build-ios.yml` | أتمتة بناء حزمة iOS وتصدير `Runner.ipa` و `Runner.app.zip` |

---

## 2. الخطوة الأولى: رفع المشروع إلى مستودع GitHub

### أ. إنشاء مستودع جديد على GitHub:
1. ادخل إلى حسابك في [GitHub](https://github.com/).
2. اضغط على علامة **`+`** أعلى الصفحة ثم اختر **`New repository`**.
3. اكتب اسم المستودع (مثال: `admin-app` أو `admin-dashboard`).
4. اختر نوع المستودع: **Private** (مستحسن للأمان) أو **Public**.
5. **مهم جداً:** لا تضع علامة صح أمام `Add a README file` أو `Add .gitignore` (لأن مشروعنا يحتوي عليها بالفعل).
6. اضغط على **`Create repository`**.

### ب. ربط ورفع المشروع من التيرمنال (Terminal / PowerShell):
افتح مجلد المشروع `e:\work\app\admin` ونفّذ الأوامر التالية بالترتيب:

```bash
# 1. تهيئة مستودع Git محلياً
git init

# 2. ضبط الفرع الرئيسي ليكون main
git branch -M main

# 3. فحص الملفات والتأكد من استبعاد .env
git status

# 4. إضافة كافة الملفات المجهزة
git add .

# 5. حفظ التغييرات بكوميت نظيف
git commit -m "feat: initialize admin project with secure environment and iOS CI/CD workflow"

# 6. ربط المستودع برابط GitHub الخاص بك (استبدل USERNAME و REPO_NAME برابطك الحقيقي)
git remote add origin https://github.com/USERNAME/REPO_NAME.git

# 7. رفع المشروع إلى GitHub
git push -u origin main
```

---

## 3. الخطوة الثانية: ضبط أسرار المستودع (GitHub Secrets)

لضمان عمل التطبيق بكامل وظائفه بعد البناء، نقوم بإضافة المتغيرات الحساسة داخل إعدادات المستودع لكي يتم حقنها تلقائياً أثناء البناء عبر `--dart-define`:

1. في صفحة مستودعك على GitHub، اضغط على تبويب **`Settings`** في الأعلى.
2. من القائمة الجانبية اليسرى، اختر: **`Secrets and variables`** ➔ **`Actions`**.
3. اضغط على الزر الأخضر **`New repository secret`**.
4. أضف المتغيرات التالية:

| اسم السر (Secret Name) | القيمة (Secret Value) | الحالة |
| :--- | :--- | :--- |
| **`SERVER_URL`** | `https://wicmn.alwaysdata.net/delever` | اختياري (مضاف كقيمة افتراضية بالـ Workflow) |
| **`MAPBOX_ACCESS_TOKEN`** | توكن Mapbox الخاص بك | اختياري (مضاف كقيمة افتراضية بالـ Workflow) |
| **`GOOGLE_MAPS_API_KEY`** | مفتاح خرائط جوجل (إن وجد) | اختياري |
| **`APP_SECRET_KEY`** | مفتاح التشفير الخاص بالتطبيق | اختياري |
| **`SDK_REGISTRY_TOKEN`** | توكن حساب Mapbox لتحميل الـ SDK | اختياري |

> [!TIP]
> إذا لم تضف أي Secrets، فإن مسار البناء سيعمل بنجاح تلقائياً لأننا وضعنا القيم الافتراضية المناسبة للعمل الفوري داخل ملف الـ Workflow والكود!

---

## 4. الخطوة الثالثة: تشغيل مسار البناء (GitHub Actions)

### أ. التشغيل التلقائي:
بمجرد تنفيذ أمر `git push` إلى فرع `main`، سيبدأ البناء تلقائياً!

### ب. التشغيل اليدوي في أي وقت (Manual Trigger):
1. توجه إلى تبويب **`Actions`** في مستودع GitHub.
2. من القائمة اليسرى اضغط على مسار العمل: **`Build iOS (No Codesign)`**.
3. ستجد زراً رمادياً على اليمين بعنوان **`Run workflow`**.
4. اختر الفرع `main` ثم اضغط على الزر الأخضر **`Run workflow`**.
5. ستشاهد الـ Action قد بدأ بالعمل (سيستغرق البناء من 7 إلى 12 دقيقة حسب سيرفرات آبل).

---

## 5. الخطوة الرابعة: تنزيل ملفات الـ Artifacts

بعد اكتمال البناء بنجاح وظهور علامة الصح الخضراء ✅:
1. اضغط على اسم البناء الذي اكتمل.
2. انزل إلى أسفل الصفحة حتى تصل إلى قسم **`Artifacts`**.
3. ستجد الملفات المتاحة للتحميل الفوري:
   - 📦 **`Runner-ipa`**: حزمة تطبيق الآيفون بصيغة `.ipa` (وهي الحزمة الأساسية للتثبيت المباشر).
   - 📦 **`Runner-app`**: حزمة `Runner.app.zip` المباشرة.
   - 📄 **`iOS-Build-Log`**: سجلات عملية البناء للاطلاع عليها في حال حدوث أي استفسار.
4. قم بتحميل ملف **`Runner-ipa`** وفك الضغط عنه لتحصل على ملف **`Runner.ipa`**.

---

## 6. الخطوة الخامسة: تثبيت التطبيق على جهاز الآيفون مجاناً

لا تحتاج إلى حساب مطور مدفوع بقيمة 99 دولار! يمكنك تثبيت ملف `Runner.ipa` على هاتفك مجاناً باستخدام أحد الطرق التالية:

### 🌟 الطريقة الأولى (الأسهل والأضمن): برنامج Sideloadly (مجاني 100%)
*يدعم أنظمة ويندوز وماك ولا يحتاج جيلبريك.*

1. حمّل برنامج **[Sideloadly](https://sideloadly.io/)** وثبّته على جهاز الكمبيوتر الخاص بك.
2. تأكد من تثبيت برنامج **iTunes** أو **Apple Devices** على الويندوز ليتعرف الكمبيوتر على جهاز الآيفون.
3. صل الآيفون بالكمبيوتر بواسطة كابل USB واختر "الوثوق بهذا الكمبيوتر" من شاشة الهاتف.
4. افتح برنامج Sideloadly، وستلاحظ ظهور اسم جهازك في خانة **`iDevice`**.
5. اسحب ملف **`Runner.ipa`** الذي قمت بتنزيله وأفلته داخل المربع الكبير في البرنامج.
6. في خانة **`Apple ID`**، اكتب إيميل حساب Apple ID العادي الخاص بك (الحساب المجاني الموجود في هاتفك).
7. اضغط على زر **`Start`** في الأسفل.
8. قد يطلب البرنامج كلمة مرور الـ Apple ID (أو رمز التحقق ذو العاملين 2FA المكون من 6 أرقام على شاشة الآيفون) لتوقيع الشهادة المجانية الشخصية.
9. انتظر دقيقة واحدة حتى تظهر رسالة **`Done.`**، وستجد أيقونة تطبيق **Admin** ظهرت فوراً على شاشة هاتفك الرئيسية!

---

### 📱 الطريقة الثانية: تطبيق AltStore
1. تثبيت **[AltServer](https://altstore.io/)** على الكمبيوتر.
2. تثبيت متجر AltStore على الآيفون.
3. فتح AltStore ➔ الضغط على علامة `+` في قسم `My Apps` ➔ اختيار ملف `Runner.ipa` ليتم التثبيت مباشرة عبر شبكة الـ Wi-Fi المنزلية.

---

### 📲 الطريقة الثالثة: أدوات التثبيت المباشر من الهاتف (بدون كمبيوتر)
إذا كنت تستخدم شهادات مخصصة أو أدوات مثل **Scarlet** أو **TrollStore** أو **ESign**:
- يمكنك فتح رابط ملف الـ IPA مباشرة من الهاتف وتثبيته بضغطة زر واحدة.

---

## 7. الخطوة السادسة: تفعيل صلاحية المطور على الآيفون

عند فتح التطبيق لأول مرة على الآيفون، ستظهر رسالة "مطور مؤسسة غير موثوق به" (Untrusted Developer)، لحلها في ثوانٍ:

1. افتح **الإعدادات (Settings)** في الآيفون.
2. اذهب إلى: **عام (General)** ➔ **VPN وإدارة الجهاز (VPN & Device Management)**.
3. تحت قسم **تطبيق المطور (Developer App)**، ستجد بريدك الإلكتروني (Apple ID)، اضغط عليه.
4. اضغط على **الوثوق في "..." (Trust "...")** ثم أكّد بالاختيار.

> [!IMPORTANT]
> **لمستخدمي iOS 16 و iOS 17 و iOS 18:**
> يلزم تفعيل نمط المطور أيضاً:
> 1. توجه إلى: **الإعدادات (Settings)** ➔ **الخصوصية والأمن (Privacy & Security)**.
> 2. انزل للأسفل حتى تجد **نمط المطور (Developer Mode)** واضغط عليه.
> 3. فعّل الخيار وأعد تشغيل الهاتف (Restart) عند طلب ذلك، ثم أكّد التفعيل.

🎉 **مبروك! تطبيق Admin يعمل الآن على جهاز الآيفون بكامل طاقته وصلاحياته.**

---

## 8. التشغيل والتطوير المحلي على جهازك

إذا أردت تشغيل وتجربة التطبيق محلياً على جهازك أو المحاكي أثناء التطوير:

```bash
# استخدام ملف البيئة المحلي مع فلاتر
flutter run --dart-define-from-file=.env
```
